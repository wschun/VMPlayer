<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/sb_backgrount"
                android:centerColor="@color/sb_backgrount"
                android:endColor="@color/sb_backgrount"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/colorMusicProgress"
                    android:centerColor="@color/colorMusicProgress"
                    android:endColor="@color/colorMusicProgress"
                    />
            </shape>
        </clip>
    </item>

</layer-list>

